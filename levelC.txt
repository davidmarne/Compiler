program Program1;
  var Apples, Oranges: Integer;
        Cost:   Boolean;
	x, y, z: float;

  procedure foo(var A: Integer);
  begin
    Write('A');
  end;

  begin
    
    x := 1;
    y := 2;
    z := 3;
    
    aidsjf := 5;

    if x = y then
    begin
      Write('x');
      if x < z then
        Write('z');
    end
    else if x < y then
      Write('y')
    else 
      Write('Fail');
   end.