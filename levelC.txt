program Program1;
  var Apples, Oranges: Integer;

  function nesting(A: Integer): Integer;

    function bar(A: Integer): Integer;

      procedure baz(var A: Integer);
      begin
        A := A + 10; 
      end;

    begin
      baz(A);
      bar := A;
    end;

  begin
    foo := A + bar(A);
  end;

begin
  Apples := 10;
  a := true;
  Write(nesting(Apples));
end.