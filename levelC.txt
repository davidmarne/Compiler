program Program1;
  var Apples, Oranges: Integer;
        Cost:   Boolean;
	x, y, z: float;

  function foo(var A, B, C: Integer; D, E: Integer): Integer;
  var X, Y: Integer;

    function bar: Integer;
    begin
      bar := 10;
    end;

  begin
    A := A + bar;
    if A < 50 then 
      foo := foo(A, B, C, D, E)
    
  end;

begin
  Oranges := 15;
  Apples := 999;
  Apples := foo(Oranges, 6, Apples, Oranges, 10);
  Writeln(Oranges);
end.